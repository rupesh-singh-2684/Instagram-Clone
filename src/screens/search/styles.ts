import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    grid: {
      backgroundColor: '#fff',
      paddingBottom: 10,
    },
    gridItem: {
      flex: 1,
      aspectRatio: 1,
      margin: 1,
    },
    image: {
      width: '100%',
      height: '100%',
      resizeMode: 'cover',
    },
  });

  export default styles